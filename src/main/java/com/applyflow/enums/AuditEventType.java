package com.applyflow.enums;

public enum AuditEventType {
    USER_REGISTERED,
    USER_LOGGED_IN,
    USER_LOGGED_OUT,
    JOB_CREATED,
    JOB_UPDATED,
    JOB_DELETED,
    JOB_STATUS_CHANGED,
    RATE_LIMIT_EXCEEDED
}
